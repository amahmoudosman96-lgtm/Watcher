# Secrets / local config — never commit. Use .env.example as the template.
.env
.env.*
!.env.example

# Local DBs (Alembic autogenerate/dev)
*.db

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/

# Eval runner output (build artifact — uploaded by CI, never committed)
eval-out/

# Node (control page)
node_modules/
.next/
